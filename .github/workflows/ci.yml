name: PHPUnit tests

on: [push, pull_request]

jobs:
  phpunit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '7.4'

      - name: Install dependencies
        run: composer install --prefer-dist --no-interaction

      - name: Configure PHPUnit
        run: vendor/bin/phpunit --configuration test/phpunit/phpunit.xml

      - name: Test dir
        run: ls -l && cd test && ls -l && cd phpunit && ls -l && ../../ && cd vendor/bin && ls -l

      - name: Run PHPUnit tests
        run: vendor/bin/phpunit ./test/phpunit/
